# Dev profile — mirrors local but intended for a shared dev environment
# spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/nthnode_auth_db
# spring.datasource.username=${DB_USER:nthnode_auth}
# spring.datasource.password=${DB_PASSWORD}

spring.flyway.enabled=false

logging.level.com.nthnode=DEBUG
