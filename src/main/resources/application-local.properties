# Local development profile — PostgreSQL via Docker Compose
# Datasource added in PR-02; properties commented here for reference
# spring.datasource.url=jdbc:postgresql://localhost:5432/nthnode_auth_db
# spring.datasource.username=nthnode_auth
# spring.datasource.password=${DB_PASSWORD:localpassword}

# Flyway disabled until PR-02 adds migrations
spring.flyway.enabled=false

logging.level.com.nthnode=DEBUG
logging.level.org.springframework.security=DEBUG
