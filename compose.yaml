services:

  auth-server:
    build: .
    ports:
      - "9000:9000"
    environment:
      SPRING_PROFILES_ACTIVE: local
      DB_URL: jdbc:postgresql://postgresql:5432/auth_db
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/auth_db
      DB_PASSWORD: ${DB_PASSWORD:-localpassword}
    depends_on:
      postgresql:
        condition: service_healthy
    restart: on-failure

  postgresql:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: auth_db
      POSTGRES_USER: auth_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-localpassword}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U auth_user -d auth_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
